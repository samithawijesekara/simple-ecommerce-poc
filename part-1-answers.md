1) System Architecture: Describe whether you would choose a monolithic or microservices-based architecture and why.
I'm choosing the microservice architecture because of this kind of system we need to scale the system in the future. So in that case I believe it's better to go with the micro services architecture. So we have ability to scale the system and balance the API requests stuffs in the microservice architecture instead of using the monolith architecture. Also if there is a team we can work in different features like auth, orders, payments using the microservices architecture.


2) Database Design: Which type of database (SQL or NoSQL) would you choose?
I'm choosing the SQL database instead of the NoSQL database for this system design because of we can have the more structured data in the SQL databases with using the relationships.


3) Rendering Approach: Would you go with server-side rendering (SSR) or a separate frontend-backend architecture? Explain your decision.


4) Technology Stack: Which frontend technology would you choose, and why (e.g., React, Vue, etc.)?
I think it's better to use React for the e-commerce websites. But rather than the React if we can go with the Next.JS that's better when considering the SEO. Because e-commerce websites should be the SEO friendly. We can breakdown into the components, can use react hooks ..etc when using the React.

Which backend technology would you choose, (e.g., Node & Express, Python & Django, Java & SpringBoot), and why?
I'm choosing the Java and SpringBoot instead of the Node and Express for this kind of e-commerce system when we considering scalability of the system. Because Java SpringBoot is more structured than the Node and Express. But if we choose the node framework like NestJs we can go with the NestJs becasue it has the more structured way to do this like same as the Java SpringBoot.


5) Cloud Infrastructure: Which cloud provider (AWS, Azure, GCP) and services would you use to deploy your platform, and why?
I'm choosing the AWS because it has more service to archive our requirements and also having the good community to having the support. When comparing into the other 2 platforms AWS is having more regions in their cloud network and cost effective.


6) Scalability: How would you design the system to handle a significant increase in traffic (e.g., Black Friday)?
We can increase the instance type into the larger instance, We can have the proper load balancing structure for this kind of special days etc..